package com.example.timetable.ui.theme

import androidx.compose.material3.Typography as MaterialTypography

// Use Material's default typography scale so text follows the platform sizing model
// and respects the user's system font-size preference consistently across the app.
val Typography = MaterialTypography()
